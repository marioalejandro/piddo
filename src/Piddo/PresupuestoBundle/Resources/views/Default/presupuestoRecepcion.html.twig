{% extends '::plantillaGerencia.html.twig' %}
 
{% block title %}
    <a href="{{ path('nuevo_presupuesto')}}">Nuevo Presupuesto</a>
{% endblock %}
{% block id 'presupuesto' %}
 
{% block article %}
    <h1>{{ block('title') }}</h1>
    <section id="formulario_cliente">
        <h4>RECEPCION DE PIEZAS</h4>
        {#<form action="{{ path('nuevo_presupuesto') }}" method="post" {{ form_enctype(form) }}>#}
        {{ form_start(form) }}
            {{ form_errors(form) }}
                <table>
                    <tr>
                        <th>Grupo</th>
                        <th>Componente</th>
                        <th>Cantidad</th>
                        <th>Máximo</th>
                    </tr>
                    {% for r in form.recepcionComponentes %}
                    <tr>
                        <td>{{ form_widget(r.grupoComponente) }}</td>
                        <td>{{ form_widget(r.componente) }}</td>
                        <td>{{ form_widget(r.cantidad) }}</td>
                        <td>{{ form_widget(r.maximo) }}</td>
                    </tr>
                    {% endfor %}
                    {% if form.recepcionComponentes == null%}
                        {{'el perfil de componentes de este motor esta vacio'}}
                    {%endif%}
                </table>
                
            {{ form_rest(form) }}
        {{ form_end(form) }}
        </form>
    </section>


{% endblock %}
 
{% block aside %}
    {# ... #}
{% endblock %}