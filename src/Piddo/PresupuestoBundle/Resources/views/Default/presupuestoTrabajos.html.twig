{% extends '::plantillaGerencia.html.twig' %}
 
{% block title %}
    <a href="{{ path('nuevo_presupuesto')}}">Nuevo Presupuesto</a>
{% endblock %}
{% block id 'presupuesto' %}
 
{% block article %}
    <h1>{{ block('title') }}</h1>
    <section id="formulario_cliente">
        <h4>RECEPCION DE PIEZAS</h4>
        {#<form action="{{ path('nuevo_presupuesto') }}" method="post" {{ form_enctype(form) }}>#}
        {{ form_start(form) }}
            {{ form_errors(form) }}
                <table>
                    <tr>
                        <th>Grupo</th>
                        <th>Componente</th>
                        <th>Cantidad</th>
                        <th>Precio</th>
                    </tr>
                    {% for tr in form.trabajos %}
                    <tr>
                        <td>{{ form_widget(tr.grupoRectificado) }}</td>
                        <td>{{ form_widget(tr.rectificado) }}</td>
                        <td>{{ form_widget(tr.cantidad) }}</td>
                        <td>{{ form_widget(tr.precio) }}</td>
                    </tr>
                    {% endfor %}
                    {% if form.trabajos == null%}
                        {{'el perfil de trabajos de este motor esta vacio'}}
                    {%endif%}
                </table>
                
            {{ form_rest(form) }}
        {{ form_end(form) }}
        </form>
    </section>


{% endblock %}
 
{% block aside %}
    {# ... #}
{% endblock %}